import { type Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact",
};

export default function Contact() {
  return <div className="flex flex-col gap-5">Contact page</div>;
}
